#include <stdio.h>
int main() { 
int c, pass;
c = 0;
pass = 0;
while ((c = getchar()) != EOF) {
  if (c == ' ') {
    while ((c = getchar()) == ' ') {
         pass = 1;
       }
  if (pass = 1) {
   putchar(' ');
    }  
  } putchar(c);
 } return 0;
}
